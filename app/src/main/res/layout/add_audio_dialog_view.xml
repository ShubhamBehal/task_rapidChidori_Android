<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/dp_20">

    <ImageView
        android:id="@+id/iv_start_recording"
        android:layout_width="0dp"
        android:layout_height="@dimen/dp_100"
        android:importantForAccessibility="no"
        android:src="@drawable/ic_record_audio"
        app:layout_constraintEnd_toStartOf="@id/iv_stop_play_recording"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/iv_stop_play_recording"
        android:layout_width="0dp"
        android:layout_height="@dimen/dp_100"
        android:layout_marginHorizontal="@dimen/dp_30"
        android:importantForAccessibility="no"
        android:src="@drawable/ic_play"
        app:layout_constraintEnd_toStartOf="@id/iv_insert_audio"
        app:layout_constraintStart_toEndOf="@id/iv_start_recording"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/iv_insert_audio"
        android:layout_width="0dp"
        android:layout_height="@dimen/dp_100"
        android:importantForAccessibility="no"
        android:src="@drawable/ic_attach"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/iv_stop_play_recording"
        app:layout_constraintTop_toTopOf="parent" />


</androidx.constraintlayout.widget.ConstraintLayout>